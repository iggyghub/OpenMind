{
  "name": "openmind-tray",
  "version": "0.1.0",
  "description": "Felix — system tray frontend for OpenMind",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "prepare": "node scripts/create-icon.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "jest": "^29.7.0",
    "pngjs": "^7.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  }
}
